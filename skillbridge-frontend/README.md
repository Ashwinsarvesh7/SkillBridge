# Note

The active Angular 21 frontend is in the **project root** (`SkillBuilding-Platform/`), not this folder.

Run from the parent directory:

```powershell
cd ..
npm install
npm start
```

This `skillbridge-frontend` folder is a legacy scaffold and is not used by `start-frontend.bat`.
